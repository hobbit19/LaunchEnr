<?xml version="1.0" encoding="utf-8"?><!-- Full screen view projects under the status bar and contains the background -->
<com.enrico.launcher3.LauncherRootView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:launcher="http://schemas.android.com/apk/res-auto"
    android:id="@+id/launcher"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <com.enrico.launcher3.dragndrop.DragLayer
        android:id="@+id/drag_layer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/workspace_bg"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:importantForAccessibility="no">

        <!-- The workspace contains 5 screens of cells -->
        <!-- DO NOT CHANGE THE ID -->
        <com.enrico.launcher3.Workspace
            android:id="@+id/workspace"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            launcher:pageIndicator="@+id/page_indicator">

        </com.enrico.launcher3.Workspace>

        <!-- DO NOT CHANGE THE ID -->
        <include
            android:id="@+id/hotseat"
            layout="@layout/hotseat"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            launcher:layout_ignoreInsets="true" />

        <!-- Keep these behind the workspace so that they are not visible when
             we go into AllApps -->
        <include
            android:id="@+id/page_indicator"
            layout="@layout/page_indicator" />

        <include
            android:id="@+id/drop_target_bar"
            layout="@layout/drop_target_bar_horz" />

        <include
            android:id="@+id/widgets_view"
            layout="@layout/widgets_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="invisible" />

        <include
            android:id="@+id/apps_view"
            layout="@layout/all_apps"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="invisible"
            launcher:layout_ignoreInsets="true" />

    </com.enrico.launcher3.dragndrop.DragLayer>

    <!-- Board -->
    <include
        android:id="@+id/board"
        layout="@layout/board"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        launcher:layout_ignoreInsets="true" />

    <include
        android:id="@+id/overview_panel"
        layout="@layout/overview_panel"
        android:visibility="gone" />
</com.enrico.launcher3.LauncherRootView>
